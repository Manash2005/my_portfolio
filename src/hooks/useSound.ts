"use client";

// This hook is currently non-functional to prevent errors.
export function useSound() {
  const playHover = () => {};
  const playClick = () => {};

  return { playHover, playClick };
}
